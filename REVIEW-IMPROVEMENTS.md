# 当前项目检查与改进（2026-09-13）

本轮基于已有应用分层实现增量修复，没有覆盖现有未提交改动。

## 已修复

- HTTP 正文限额由只读 Content-Length 改为 ASGI 实际字节预算。分块传输、缺少长度和虚报短长度均不能绕过；正文超过预算立即返回 413，不进入路由。预算内完整正文可继续解析。
- 声明长度先限定数字格式和长度再转换，避免超长数字触发异常。
- X-Request-ID 只接受 1–64 个字母、数字、点、下划线或连字符；控制字符和其它值替换为服务端 ID。
- 提前拒绝的 403/413 同样带安全头与请求 ID；v1 使用统一错误信封，旧端点保留 detail 兼容。
- RunsService 在关闭开始时停止准入，避免等待既有任务期间再创建运行；拒绝不产生消息、Run 或事件。
- make test / make check 明确执行离线套件，真实模型测试保留在 make test-live。安装密钥不再改变常规检查是否访问外部模型。

## 验证与边界

新增 3 项回归测试覆盖多种输入和关闭竞争；针对性套件 69 项通过。全量离线测试 597 项通过；ruff、104 个文件格式检查及 git diff --check 通过。

第一次 make check 按原配置触发了真实模型测试，已中止：当时 2 项通过、3 项失败，不能声称真实模型验收完成。失败包括诱导案例被核验撤回、正常回答包含非四柱干支、未明确体系时模型先追问。后两类断言需要与目前命盘字段和澄清行为核对，前者需要检查命盘依据与核验覆盖；本轮未修改这些断言以掩盖失败。

仍为单进程内存实现，不具备重启持久化。关闭时取消后的 gather 仍依赖执行器配合取消，尚未解决恶意/失控执行器吞掉取消时的硬截止问题。应用层数据库、幂等、执行隔离、账户和删除补偿仍按 backend_doc.md 分阶段推进。
