# M0 技术假设验证结论

> 状态：**已完成** | 日期：2026-09-12
> 环境：Python 3.12.14 (uv) · lunar-python 1.4.8 · kerykeion 5.12.9 · pyswisseph 2.10.3.2

M0 的目的是在写业务代码之前打掉承重假设。结论直接修正了 M1 的工作量估计，
并发现 3 个会静默出错的坑。

---

## 结论摘要

| 假设 | 结论 | 对计划的影响 |
|---|---|---|
| **H1** lunar-python 八字能力 | ✅ **覆盖充分，超出预期** | 大运/流年**无需自研**，M1 工作量下调 |
| **H2** 真太阳时校正 | ✅ **已实现并验证** | 证实不校正会静默排错时柱 |
| **H3** 节气交接精度 | ✅ **±2 秒，与 Swiss Ephemeris 一致** | **sxtwl 从依赖树移除** |
| **H4** 星盘侧 (kerykeion) | ⚠️ **可用，但有 3 个坑** | M3 需针对性处理 |

---

## H1 — lunar-python 八字能力边界

**结论：README 严重低估了自身能力。大运/流年/流月全部支持，无需自研。**

### 已覆盖（直接可用）

| 能力 | API |
|---|---|
| 四柱、十神（天干 + **地支藏干**） | `getYear/Month/Day/TimeShiShenGan` / `...Zhi`（Zhi 返回列表） |
| 地支藏干 | `getXHideGan()` |
| 纳音、旬、旬空 | `getXNaYin()` / `getXXun()` / `getXXunKong()` |
| **十二长生（地势）** | `getXDiShi()` |
| 胎元、命宫、身宫、胎息（+ 各自纳音） | `getTaiYuan()` / `getMingGong()` / `getShenGong()` / `getTaiXi()` |
| **大运（含精确起运时间）** | `getYun(gender)` → `getDaYun()` |
| **流年 → 流月** | `DaYun.getLiuNian()` → `LiuNian.getLiuYue()` |
| 节气表、前后节气 | `Lunar.getJieQiTable()` / `getPrevJieQi()` |
| 流派（晚子时处理） | `setSect(1\|2)` |

**准确性已独立核对**：1990-06-15 10:30 男 → 四柱 `庚午 壬午 辛亥 癸巳`。
用五虎遁（庚年正月起戊寅 → 五月壬午）与五鼠遁（辛日子时起戊子 → 巳时癸巳）
手工推算，与库输出**完全一致**。大运顺排 `癸未→甲申→乙酉→丙戌→丁亥` 亦吻合。

### 未覆盖（必须自建）

1. **八字神煞** —— 天乙贵人 / 桃花 / 驿马 / 华盖 / 羊刃 / 文昌 等。
   注意 `Lunar.getDayJiShen()` / `getDayXiongSha()` 是**黄历择日**层面的，**不是八字神煞**，
   不可混用。→ 机械的规则表工作，照古籍建表即可。
2. **五行旺衰强弱评分** —— 月令得分、通根、透干、合化。
   → **这是真正的智力工作，且高度依赖流派**，正是计划中把流派做成配置的原因。

---

## H2 — 真太阳时校正

**结论：已实现（`spikes/h2_true_solar_time.py`，M1 迁移至 `engines/bazi/truesolar.py`）。
证实不做校正会静默产出错误时柱。**

### 实现

```
地方平太阳时 = UTC + 经度/15 小时
真太阳时     = 地方平太阳时 + 均时差
UTC          = 钟表时间 − 该时刻实际时区偏移   # 自动吸收夏令时，无需特殊分支
```

均时差用 `swe.time_equ(jd)`（pyswisseph），**比自写近似公式更准**，且我们已有该依赖。
验证：11月初 +16.45min、2月中 −14.19min、5月中 +3.65min，与天文年历吻合。

### 核心证据（本项目最有说服力的验证）

同一钟表时间 **1990-06-15 10:30**，不同出生地：

| 出生地 | 经度 | 真太阳时 | 时柱变化 |
|---|---|---|---|
| 北京 | 116.40°E | 09:15 | 癸巳 → 癸巳（不变） |
| 上海 | 121.47°E | 09:35 | 癸巳 → 癸巳（不变） |
| **乌鲁木齐** | 87.62°E | **07:20** | **癸巳 → 壬辰（改变）** |
| **拉萨** | 91.14°E | **07:34** | **癸巳 → 壬辰（改变）** |

**不做校正，西部城市出生的时柱从源头就是错的，且用户无从察觉。**

### 其他已验证项

- **中国夏令时 1986–1991**：正确识别为 UTC+9（6 个区间已硬编码）
- **跨日**：乌鲁木齐凌晨出生正确触发，真太阳时落到前一日 → **日柱需按前一日推算**
- **均时差检验**：3/3 通过

### 遗留（M1 处理）

- DST 起始日 02:00–03:00 是**不存在的时间**，当前未拦截，应报错或提示
- 1949 年前中国分五时区，当前不做推断，需调用方保证输入

---

## H3 — 节气交接精确时刻

**结论：lunar-python 节气精度 ±2 秒，与 Swiss Ephemeris 视黄经真值一致。sxtwl 确认冗余，已移除。**

方法：用 `swe.solcross_ut(deg, jd, FLG_SWIEPH)` 求太阳视黄经到达 15° 整数倍的精确时刻，与
`Lunar.getJieQiTable()` 逐项比对。

**自洽性检验**：在 `solcross_ut` 求得的时刻，太阳视黄经 = `360.000000000°`（机器精度）。
对比"真黄经"（去章动）偏离 4.376 角秒 ≈ 105 秒 —— 证明该函数算的是**视黄经**，正是节气的正确定义。

1990 年 24 节气全部比对，**最大偏差 2 秒**。

> 附带收益：`sxtwl` 的 PyPI sdist 缺 `src/JD.cpp` 无法构建。H3 证明 pyswisseph
> 足以承担节气计算，**该依赖已从技术栈移除**，少一个编译失败风险的 C++ 扩展。

---

## H4 — 星盘侧 (kerykeion) 附加验证

计划外补充验证（因星盘也在 MVP 内，提前打掉 M3 风险）。

**星盘计算正确**：1990-06-15 10:30 北京 → 太阳 23.71° 双子 ✓（天文预期 ~24°）、
金星 18.26° 金牛、火星 10.73° 白羊、上升 21.86° 狮子。

**配置项与计划完全对应**：`houses_system_identifier='P'`(Placidus)、
`zodiac_type='Tropical'`、`sidereal_mode='LAHIRI'`、`online=False` 可离线。

### ⚠️ 发现 3 个坑（M3 必须处理）

1. **不传 `city`/`nation` 会默认填 `Greenwich` / `GB`**
   —— 经纬度是出生地的，但出生地名变成伦敦。查证：序列化上下文里赫然写着
   `<birth_data city="Greenwich" nation="GB" lat="39.90" lng="116.41" .../>`。
   **LLM 会读到错误出生地**。→ 强制显式传参。

2. **SVG 默认写入用户主目录**，不是当前工作目录。
   → 必须显式传 `new_output_directory=`。

3. **`chart_language` 正确值是 `"CN"` 不是 `"ZH"`**，且传错**不报错、静默回落英文**。
   ```python
   KerykeionChartLanguage = Literal["EN","FR","PT","IT","CN","ES","RU","TR","DE","HI"]
   ```
   已实测 `"CN"` 生效：月亮/水星/金星/火星/木星/上升 均为中文，英文残留为 0。

### 其他可用能力（超出计划预期）

`to_context()` 提供 **AI Context Serializer**（4.4KB 结构化 XML，含 `<config>` 段声明
黄道制与宫位制 —— 正好满足"解读需标注所用流派"的要求）；另有 `ReportGenerator`、
`AspectsFactory`、`TransitTimeRangeFactory`、`RelationshipScoreFactory`（合盘）、
`PlanetaryReturnFactory`（返照盘）。

---

## 对后续计划的影响

1. **M1 工作量下调** —— 大运/流年/流月复用现成库，只需自建神煞规则表。
2. **M1 新增两项** —— 五行旺衰评分引擎（流派相关）、DST 边界时间校验。
3. **M3 新增三项** —— city/nation 强制校验、SVG 输出目录、`chart_language="CN"`。
4. **依赖树简化** —— 移除 `sxtwl`；确定技术栈为 `lunar-python` + `kerykeion` + `pyswisseph`。

## 验证方式（可复现）

```bash
cd metaphys/backend
./.venv/bin/python spikes/h2_true_solar_time.py    # H2，退出码 0 表示均时差检验通过
```

H1/H3/H4 的验证为一次性探针，结论已固化于本文档。
M1 阶段将把这些结论转为 `backend/tests/` 下的正式回归测试。
