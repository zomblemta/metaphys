# M0–M3 后设计审查修复

日期：2026-09-12。范围：修复设计审查发现的五项问题；尚未实现 M4 网关/前端与 M5 安全判定。

## 已改变的行为

### 1. 错误答复撤回

`GroundingMiddleware` 命中既有核验规则后，按原 AIMessage 的 id 替换正文为明确的降级答复，同时保留工具调用协议与更正消息。本轮不会继续向用户返回原始错误解读，也不会无限重试模型。

审计 flags 追加 `message_id`。`RunResult.as_dict()` 新增：

- `response_status`：`withheld` 表示当前答复被核验撤回，`completed` 表示运行完成（不等于所有事实均已被验证）。
- `response_flags`：仅当前答复的核验命中。
- 原 `grounding_flags` 保留为历史审计记录，不用它判断当前回复状态。

既有星盘结构句式覆盖范围保持不变；无对应盘的泛指内容仍不作核验。修复的是“已经发现错误，却仍原样输出”，不宣称能识别所有编造。误报现在也会撤回解读，因此既有误报边界仍需关注。

### 2. 当前命盘资料一致性

`merge_charts` 对比规范化公历钟表时间、经纬度、时间精度与称呼。相同资料允许八字与星盘共存；资料变化时移除其它不匹配的盘。八字使用引擎计算的 `clock_time`，因此农历八字与等价公历星盘仍可以共存。性别属于八字独有计算参数，不用于使星盘失效。

- `None` 是显式删除某盘种；空更新仍是无变更。
- 称呼变化保守地视为资料变化；改称呼后另一种盘需要重新排出。
- 每次模型调用注入当前有效盘上下文，说明历史 ToolMessage 中其它盘已失效；历史消息仍保留用于追溯。
- 未引入多命主管理界面。当前状态表达一份有效出生资料，历史消息不是可同时分析的多命主档案。

### 3. 明确当地时间契约

`BirthProfile` 拒绝带 Z 或 UTC 偏移的出生时间。`china_time` 的公共换算入口也拒绝带时区 datetime，不再静默删掉 tzinfo。两个工具将契约错误转成需核对当地时间的追问。

本次选择“仅接收出生地无偏移钟表时间”，没有自行实现全球时区转换。工具文档和提示词明确禁止模型直接删除用户的偏移字符。

### 4. 历史近似时间的确认出口

保持原有占位时间防护。用户核对后若确实是同一个时刻，可发送工具追问中提供的确认句，例如：

```
确认出生时间：1990-06-15 10:30:00
```

工具仅检查当前最近一条 HumanMessage 的完整确认句；不采信模型生成的确认、旧轮确认、近似说法或不匹配时间。确认后可将相同时间升级为 exact 并排星盘，旧精度下的八字失效。自然语言确认不作模糊猜测，助手会提供可复制的确认句。

### 5. 公开流式事件 v1

`astream_run` 仍支持逐步进度，但不再透传原始图状态或 AIMessage/ToolMessage 对象。

进度示例：

```json
{"event":"update","schema_version":1,"run_id":"<UUID>","node":"model","data":{"status":"running"}}
```

终态示例：

```json
{"event":"final","schema_version":1,"run_id":"<同一 UUID>","data":{"reply":"…","response_status":"completed","response_flags":[],"charts":{},"birth_profile":null,"grounding_flags":[],"safety_flags":[],"message_count":2}}
```

所有事件可直接 JSON 序列化；只有终态包含答复。`node` 是诊断信息，前端不应依赖内部中间件节点名称进行业务判断。公开 update 不携带工具消息正文、工具参数或 svg_path。

**M4 的资源访问尚未实现。** SVG 仍由内部工具生成，后续必须实现会话/用户归属校验与受控下载路由；当前修复不构成 HTTP 鉴权交付。也没有新增公开静态目录。

## 实测与回归

命令：`cd backend && make check`。

结果：**451 passed、15 skipped**；ruff check 与 62 文件格式检查通过。15 项真实模型对抗测试缺少真实密钥，未执行；有 1 条 kerykeion 第三方弃用警告。

新增 `backend/tests/test_design_regressions.py`，23 项回归覆盖：

- 错误正文撤回、下一轮不受旧审计污染、携带工具调用的错误正文不破坏调用协议。
- 异步流每个事件都能序列化，核验前不释放正文。
- 日期、坐标、精度、称呼变化时旧盘失效；相同资料与等价农历/公历资料共存；显式删除。
- 真实多轮图中八字→星盘和星盘→八字两个方向的资料更新。
- Z、+08:00、夏令时 +09:00 输入在模型契约、换算入口和两种工具路径均被拒绝。
- 确认句的正反样本、旧轮/助手确认不生效，以及真实多轮图的相同时刻精度升级。

更新了一项旧测试：原先断言“换命主称呼仍保留旧八字”，现在要求旧盘失效。中间件数量与图节点数量未增加；现有递归上限检查仍通过。

业务修改保留在工作区，未提交、未部署。下一步可基于此公开事件契约推进 M4，但需补齐资源鉴权及 M5 上线前安全逻辑。
