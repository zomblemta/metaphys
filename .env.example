# 复制为 .env 并填入真实值。config.yaml 里的 $DEEPSEEK_API_KEY 会在加载时展开，
# 缺失即报错 —— 不会静默变成空字符串。
#
# 注意：M2 的绝大部分测试**不需要** key（工具、中间件、Grounding、装配全部可
# 确定性验证）。只有「对抗性测试 —— 真实模型是否仍调用工具」这一条需要。
DEEPSEEK_API_KEY=sk-your-key-here
