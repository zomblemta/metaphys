# Copy to .env.production; never commit the real file.
# POSTGRES_PASSWORD: generate with python -c 'import secrets; print(secrets.token_hex(32))'
POSTGRES_PASSWORD=replace-with-random-hex-password
METAPHYS_DOMAIN=metaphys.example.com
DEEPSEEK_API_KEY=replace-with-real-key
