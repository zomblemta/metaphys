# M1 排盘引擎 —— 交付与发现

> 状态：**M1 已完成** | 日期：2026-09-12
> 144 项测试通过 · ruff check 与 ruff format 全绿

M1 的目标是「给定出生信息输出稳定正确的命盘 JSON」。引擎已可交付，
且过程中发现了一个**会静默错盘的架构问题** —— 比 M0 的三个坑更严重。

---

## 一、交付物

| 文件 | 职责 |
|---|---|
| `metaphys/schemas/chart.py` | Pydantic 契约：`BirthProfile` / `BaziChart` / `Pillar` / `DaYun` / `School` / `ShenSha` / `StrengthAnalysis` |
| `metaphys/engines/bazi/truesolar.py` | 真太阳时校正、均时差、夏令时（含缺口拦截） |
| `metaphys/engines/bazi/ganzhi.py` | 五行/阴阳/十神/藏干/十二长生基础表 + 五行关系查询 |
| `metaphys/engines/bazi/chart.py` | 排盘主流程：历法归一 → 双时钟域 → 四柱 → 大运 → 神煞/旺衰 |
| `metaphys/engines/bazi/shensha.py` | 神煞规则表（天乙/文昌/羊刃/桃花/驿马/华盖/将星/劫煞/亡神） |
| `metaphys/engines/bazi/strength.py` | 五行旺衰评分，权重按流派取预设 |
| `metaphys/engines/geo/` | 离线城市经纬度（3332 条，含歧义消解） |
| `tests/test_bazi.py` | 核心回归集 |
| `tests/test_shensha_strength.py` | 神煞表与旺衰评分，74 项 |
| `tests/test_harness_boundary.py` | 架构护栏 |

**验证**：`cd metaphys/backend && ./.venv/bin/python -m pytest -q`

---

## 二、核心发现：八字需要**两个时钟域**

这是 M1 最重要的结果，直接改变了引擎结构。

### 问题

初版把整盘都按真太阳时推算（多数软件的做法）。测试暴露了矛盾：

> 2024-02-04 17:30 出生，北京与乌鲁木齐 **是同一瞬间**（全国统一北京时间），
> 但真太阳时相差 2.16 小时 —— 一个已过立春、一个未过。
> 于是同一瞬间出生的人，排出了**不同的年柱与月柱**。

这显然是错的。**节气是太阳到达特定视黄经的全球性瞬间**，不因观测地而改变。

### 结论

| 柱 | 决定因素 | 时钟域 |
|---|---|---|
| **年柱、月柱** | 节气（全球性天文瞬间） | **绝对时刻**（换算回北京标准时） |
| **日柱、时柱** | 当地真太阳时 | **出生地真太阳时** |

大运由月柱推得、起运取决于到节气的距离，同属绝对时刻域。

### 为什么这一条重要

* 整盘用真太阳时 → 靠近节气出生者的**月柱随经度漂移**（乌鲁木齐偏 2.16 小时），静默错盘。
* 整盘用绝对时刻 → **西部城市的时柱从源头就错**，正是 M0 论证过的问题。

两者都错，必须拆开。回归测试 `test_same_instant_near_solar_term` 钉死了这一点。

---

## 三、其他发现与修复

1. **只有「节」换月柱，中气不换。**
   `getPrevJieQi()` 会返回中气（如大寒）。据它在附近出生告警即**误报** ——
   在大寒前后出生，月柱根本不变。已改为只在十二「节」上判定。

2. **只看前一个节气会漏掉最危险的情形。**
   出生在交节**之前**时，换柱的是**即将到来**的那个节。原实现只查 prev，
   恰好漏掉最需要预警的场景。已改为取前后两侧中最近的一个节。

3. **lunar-python 节气表用拼音键区分重名。**
   表跨约 15 个月，同名节气第二次出现时改用拼音大写键
   （2024 立春 = `"立春"`，2025 立春 = `"LI_CHUN"`）。
   只按中文名查找会**静默漏掉一半的节**。已在 `_JIE_NAMES` 中同时容纳两种形式。

4. **夏令时缺口内的时刻不存在。**
   起始日 02:00 直接跳到 03:00，`02:00–03:00` 无对应真实时刻。
   原先会照常排盘（用户在那一刻并未出生）。改为抛 `NonexistentLocalTimeError`，
   交由上层追问。结束日的重复小时（01:00–02:00 出现两次）则标注后按标准时解释。

5. **农历闰月必须显式声明。**
   lunar-python 用**负月份**表示闰月。若不区分，闰月会被当作普通月静默解析成
   完全错误的日期（2023 二月初一 = 02-20，闰二月初一 = 03-22，差一个月）。
   `BirthProfile.is_leap_month` 驱动；闰月不存在时报 `InvalidLunarDateError`。

6. **十神与地势必须按日主重算。**
   年/月柱与日/时柱取自不同时刻后，lunar-python 内部算出的十神是相对**它自己那盘**
   的日主，会张冠李戴。已在 `ganzhi.py` 自建表并一律重算 —— 与 lunar-python
   的输出逐项核对一致。

7. **异常类型收窄。**
   lunar-python 对非法农历日期抛裸 `Exception`，漏出去会让调用方无法区分
   "用户输入有误"与"引擎崩溃"。已收窄为 `InvalidLunarDateError(ValueError)`。

---

## 四、神煞与旺衰：如何避免"装作有唯一正解"

这两块是八字里**最依赖流派**的部分 —— 同一张盘，子平、盲派、新派可能给出不同的
强弱判断，进而推出完全相反的喜忌。因此设计上不做两件事：

**不做**：给出一个不带前提的"身弱"结论。
**而做**：把评分拆成可复核的分量全部输出，并标注所用流派。

于是 `StrengthAnalysis` 同时携带：五行的分与占比、同党/异党总分与占比、
**通根与否及通在哪几柱**、**透干在哪几柱**、以及所用流派的取法依据。
解读层因此可以说"依子平法，月令司权判为身弱"，专业读者能复核前提。

* **权重预设**：`_ZIPING` 月支 5.0（月令司权最重）· `_MANGPAI` 月 3.5 / 日 3.0
  （盲派重日主坐支）· `_XINPAI` 各柱均衡。三者在同一张盘上确实给出不同占比 ——
  有测试专门钉住这一点，否则流派配置形同虚设。
* **通根单独标记**：日主在地支藏干中出现即为有根，这是身强身弱的关键分野，
  不能只靠总分掩盖。三处以上通根、完全无根，都会额外写入 `notes`。
* **日干不计分**：日干即日主本身，计入即是自己给自己加分。

神煞同理，每条命中都记 `base`（以日干为准 / 以年支为准 / 以日支为准）与
`pillar`。三合局类神煞**年支与日支两种查法并用**，结论不同时分别保留而非二选一 ——
基准本身就是信息。

**羊刃取十二长生的帝旺位**，而非硬编码。这样阳干（甲卯、丙戊午、庚酉、壬子）
与通行口诀完全吻合，阴干则依「阳顺阴逆」自动落到乙寅、丁己巳、辛申、癸亥。
有测试断言"羊刃集合"恒等于"帝旺集合"，两者永不自洽失效。

**注意**：`Lunar.getDayJiShen()` / `getDayXiongSha()` 是**黄历择日**层面的吉凶神煞，
与八字神煞同名异物，不可混用（M0 已确认，故自建规则表）。

---

## 五、测试覆盖

144 项，按"会让产品静默出错的场景"组织：

| 组 | 覆盖 |
|---|---|
| 真太阳时 | 均时差极值与天文年历比对 · 经度改变时柱 · **同一瞬间年月柱不变** |
| 夏令时 | 1986–1991 识别 · **缺口拦截与边界** · 重复小时 |
| 节气边界 | 立春换年柱 · **交节前后对称告警** · **中气不误报** |
| 历法 | 农历锚点 · 闰月与普通月差异 · 非法日期报错 |
| 精度降级 | 时辰未知→三柱 · **`clock_time` 不泄漏内部占位值** · 三柱仍可算旺衰 |
| 完整性 | 已知锚点四柱 · 十神/藏干/纳音/地势 · 大运顺逆 · 可复现性 |
| 神煞 | 天乙/文昌/三合局六煞逐支对照口诀 · **羊刃≡帝旺** · 基准与柱位记录 · 去重与稳定排序 |
| 旺衰 | **同党+异党=总分** · 分党穷尽五行 · 流派确实改变结果 · 通根/透干检出 · 判语单调 |
| 架构护栏 | harness 不依赖 app · **引擎不依赖任何 LLM/网络库** |

---

## 六、对后续的影响

1. **M2 工具层**须把两个新异常（`NonexistentLocalTimeError` / `InvalidLunarDateError`）
   映射为追问，而非报错 —— 这正是 `ClarificationMiddleware` 的用武之地。
2. **解读层**须读 `time_accuracy` 与 `warnings` 降级表述：时辰未知时不得谈时柱十神，
   贴近节气时须声明月柱可能不准。
3. **解读层须标注流派前提**。旺衰结论离开流派无意义 —— prompt 中必须带上
   `strength.school` 与 `strength.notes[0]` 的取法依据，否则等于把一家之言
   说成定论，正是专业用户一眼看穿的地方。
4. **M3 星盘**不存在双时钟域问题 —— 星盘全部基于绝对时刻，与出生地经度是另一套关系。
5. **`school` 需从配置贯通到工具参数**。目前 `compute_bazi(school=...)` 已就绪，
   但 `config.yaml` 的 `bazi.school`（计划 4.2）尚未接线。
6. 遗留整理项：`spikes/_svg_out/` 加入 `.gitignore`。**（M2 已处理）**

M2 另见 [`M2-FINDINGS.md`](M2-FINDINGS.md)。
